"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = (element, nodeName, attribute, value) => {
    if (!element || element.nodeName.toLowerCase() !== nodeName.toLowerCase()) {
        return false;
    }
    const relAttribute = element.getAttribute(attribute);
    if (!relAttribute) {
        return false;
    }
    const rels = relAttribute.toLowerCase()
        .split(' ');
    return rels.some((rel) => {
        return rel === value;
    });
};
