"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_debugging_protocol_common_1 = require("@hint/utils-debugging-protocol-common");
const chrome_launcher_1 = require("./chrome-launcher");
class ChromeConnector extends utils_debugging_protocol_common_1.Connector {
    constructor(server, config) {
        const launcher = new chrome_launcher_1.CDPLauncher(config && config.launcherOptions || {});
        super(server, config || {}, launcher);
    }
}
exports.default = ChromeConnector;
ChromeConnector.schema = {
    additionalProperties: false,
    properties: {
        ignoreHTTPSErrors: { type: 'boolean' },
        launcherOptions: { type: 'object' },
        waitFor: {
            minimum: 0,
            type: 'number'
        }
    }
};
