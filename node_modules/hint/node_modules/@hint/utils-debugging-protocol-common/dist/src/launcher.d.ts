import { BrowserInfo, ILauncher, LauncherOptions } from 'hint';
export declare abstract class Launcher implements ILauncher {
    protected pidFile: string;
    protected port?: number;
    protected _options: LauncherOptions;
    readonly options: LauncherOptions;
    constructor(options: LauncherOptions);
    abstract launch(url: string): Promise<BrowserInfo>;
}
//# sourceMappingURL=launcher.d.ts.map